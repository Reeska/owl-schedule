{
  "name": "owl-schedule",
  "version": "1.0.0",
  "author": "Reeska <thchampi@gmail.com>",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "app",
    "functions"
  ],
  "scripts": {
    "predeploy": "firebase login",
    "deploy": "firebase deploy",
    "deploy:app": "yarn deploy --only hosting",
    "deploy:api": "yarn deploy --only functions"
  },
  "dependencies": {
    "firebase-tools": "latest"
  }
}
