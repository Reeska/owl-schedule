import React from 'react'
import styled from 'styled-components'

const Paragraph = styled.p`
  margin: 60px 0;
  font-style: italic;
`

const UiNoMatch = () => (
  <Paragraph>No matches this week.</Paragraph>
)

export default UiNoMatch
